version: '3.8'

services:
  postgres_db:
    image: postgres:latest
    container_name: postgres_db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: Senha1234
      POSTGRES_DB: N_Bank_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - nbank_network
    restart: always

  flask_app:
    build:
      context: .
      dockerfile: dockerfile
    container_name: flask_app
    ports:
      - "80:80"
    environment:
      FLASK_ENV: development
    depends_on:
      - postgres_db
    networks:
      - nbank_network
    restart: always


volumes:
  postgres_data:

networks:
  nbank_network:
    driver: bridge